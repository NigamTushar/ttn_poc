import 'package:flutter/material.dart';

class Absences extends StatelessWidget {
  const Absences({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Absences'));
  }
}
